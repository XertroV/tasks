---
title: Common Questions
---

## Why `bl` in docs?

`bl` is the canonical form for brevity. `backlog` and `tasks` are equivalent aliases.

## How is docs drift handled?

Reference metadata is generated from CLI help at build time.
